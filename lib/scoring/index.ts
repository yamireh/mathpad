/**
 * Scoring — placeholder.
 *
 * The real scoring logic compares recognised answer-box digits against each
 * question's correct answer to mark a session. Per SPEC.md, marking depends
 * exclusively on the answer boxes — never on the kid's scratch work. Not
 * implemented in the foundation phase.
 */
export {};
